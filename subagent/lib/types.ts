/**
 * Sub-agent types — re-exported from the runtime-agnostic
 * `@earendil-works/pi-subagent-core` shared with the DeepSeek Harness
 * worktree provider plugin.
 */
export type {
	ResolvedModel,
	SpawnSubagentOptions,
	SubAgent,
	SubAgentStatus,
	SubAgentUsage,
	SubagentRunRequest,
	SubagentRunResult,
} from "./core/index.ts";
